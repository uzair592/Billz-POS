'use client';
import {useQuery,useQueryClient} from '@tanstack/react-query';import{useState}from'react';import{Button,Card,Input,Notice}from'../../../components/ui';import{api}from'../../../lib/api';
type Branch={id:string;name:string;code:string;address?:string;phone?:string;isPrimary:boolean;isActive:boolean};
export default function Branches(){const qc=useQueryClient();const q=useQuery({queryKey:['branches'],queryFn:()=>api<Branch[]>('/branches')});const[open,setOpen]=useState(false);const[message,setMessage]=useState('');const[form,setForm]=useState({name:'',code:'',address:'',phone:'',timezone:'Asia/Karachi',isPrimary:false});async function submit(e:React.FormEvent){e.preventDefault();try{await api('/branches',{method:'POST',body:JSON.stringify(form)});setOpen(false);await qc.invalidateQueries({queryKey:['branches']});}catch(error){setMessage(error instanceof Error?error.message:'Unable to create branch.');}}return <><header className="topbar"><div className="page-title"><h1>Branches</h1><p>Locations available to your team</p></div><Button onClick={()=>setOpen(!open)}>Add branch</Button></header>{open&&<Card><h2>New branch</h2>{message&&<Notice>{message}</Notice>}<form onSubmit={submit}><div className="form-grid"><Input label="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><Input label="Code" value={form.code} onChange={e=>setForm({...form,code:e.target.value.toUpperCase()})}/><Input label="Address" value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><Input label="Phone" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><Button>Create branch</Button></form></Card>}<Card><div className="table-wrap"><table className="data-table"><thead><tr><th>Name</th><th>Code</th><th>Address</th><th>Status</th></tr></thead><tbody>{q.data?.map(b=><tr key={b.id}><td>{b.name}{b.isPrimary?' · Primary':''}</td><td>{b.code}</td><td>{b.address||'—'}</td><td>{b.isActive?'Active':'Inactive'}</td></tr>)}</tbody></table></div>{q.data?.length===0&&<p className="muted">No branches yet.</p>}</Card></>;}
