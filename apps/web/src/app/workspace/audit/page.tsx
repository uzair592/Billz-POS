'use client';
import{useQuery}from'@tanstack/react-query';import{Card,Notice}from'../../../components/ui';import{api}from'../../../lib/api';
type Log={id:string;action:string;entityType:string;entityId?:string;createdAt:string;ipAddress?:string;reason?:string};
export default function Audit(){const q=useQuery({queryKey:['audit'],queryFn:()=>api<{items:Log[]}>('/audit-logs?take=50')});return <><header className="topbar"><div className="page-title"><h1>Audit log</h1><p>Append-only security and configuration activity</p></div></header>{q.error&&<Notice>{q.error.message}</Notice>}<Card><div className="table-wrap"><table className="data-table"><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>IP address</th><th>Reason</th></tr></thead><tbody>{q.data?.items.map(log=><tr key={log.id}><td>{new Date(log.createdAt).toLocaleString()}</td><td>{log.action}</td><td>{log.entityType}</td><td>{log.ipAddress||'—'}</td><td>{log.reason||'—'}</td></tr>)}</tbody></table></div>{q.data?.items.length===0&&<p className="muted">No audit events recorded yet.</p>}</Card></>;}
